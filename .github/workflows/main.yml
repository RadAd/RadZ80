name: add-date-tag
on: [workflow_dispatch]
jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - run: |
        set _=%DATE%
        echo DATE %DATE%
        echo _ %_%
        
      shell: cmd
